import React from "react";

const Spinner = () => {
  return (
    <img src={"../assests/spinner.gif"} alt="" style={{width : '100px', margin : 'auto', display : 'block'}} />
    );
};

export default Spinner;
